<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Sandbox required for Mac App Store submission -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Allow user to select and read/write files via open/save dialogs -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow outbound network connections (AI APIs, iCal feeds, HuggingFace downloads) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow security-scoped bookmarks to persist access to vault folders selected by the user -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>
</dict>
</plist>

